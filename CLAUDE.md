# پروژه: اتاق‌های گفتگوی فرهنگ و هنر

## توضیح کوتاه
یک وب‌سایت فارسی که برای هر اثر فرهنگی و هنری (کتاب، موسیقی، پادکست) یک «اتاق» اختصاصی دارد.
کاربران وارد اتاق هر اثر می‌شوند و درباره‌ی آن می‌نویسند، نظر می‌دهند و به نظر یکدیگر پاسخ می‌دهند.

مخاطب: فارسی‌زبان، داخل ایران.
زبان رابط کاربری: فارسی، چیدمان راست‌چین (RTL).

## تکنولوژی‌ها
- Backend و Frontend: Django (تمپلیت‌های خود Django — بدون React یا SPA)
- دیتابیس در توسعه: SQLite
- دیتابیس در انتشار: PostgreSQL روی لیارا
- استایل: Tailwind CSS با پشتیبانی RTL
- فونت: وزیرمتن (Vazirmatn)
- مدیریت محتوا: Django Admin

## اصول مهم این پروژه
- تمام متن‌های نمایشی به فارسی نوشته شوند.
- چیدمان کل سایت `dir="rtl"` باشد.
- تاریخ‌ها به شمسی نمایش داده شوند (کتابخانه‌ی `django-jalali-date` یا معادل آن).
- اعداد نمایشی فارسی باشند.
- نسخه‌ی اول بدون سیستم ثبت‌نام است. کاربر فقط یک نام دلخواه وارد می‌کند.
- کد باید ساده و خوانا باشد. از انتزاع و لایه‌بندی غیرضروری پرهیز شود.
- هر قابلیت جدید در کوچک‌ترین حالت ممکن پیاده شود.

## ساختار داده

### Category (دسته‌بندی)
- `name` — نام دسته (کتاب، موسیقی، پادکست)
- `slug` — شناسه‌ی انگلیسی برای URL
- `icon` — نام آیکون (اختیاری)

### Work (اثر)
- `title` — نام اثر
- `slug` — شناسه‌ی انگلیسی برای URL
- `category` — کلید خارجی به Category
- `creator` — نام پدیدآورنده (نویسنده، خواننده، سازنده)
- `year` — سال انتشار (اختیاری)
- `description` — معرفی کوتاه اثر
- `cover_image` — تصویر جلد (اختیاری)
- `is_published` — نمایش داده شود یا نه
- `created_at`

### Comment (نظر)
- `work` — کلید خارجی به Work
- `author_name` — نام دلخواه نویسنده‌ی نظر
- `body` — متن نظر
- `parent` — کلید خارجی به خودِ Comment برای پاسخ‌ها (اختیاری، null)
- `is_approved` — تأیید شده یا نه
- `created_at`

نکته: `parent` باعث می‌شود ساختار پاسخ‌دهی درختی شکل بگیرد. در نسخه‌ی اول فقط یک سطح پاسخ کافی است.

## صفحات
1. صفحه‌ی اصلی — نمایش دسته‌بندی‌ها و چند اثر تازه یا پرگفتگو
2. صفحه‌ی دسته‌بندی — فهرست آثار یک دسته
3. صفحه‌ی اثر (اتاق) — معرفی اثر + فهرست نظرات + فرم ارسال نظر
4. صفحه‌ی جستجو — جستجوی ساده در نام اثر و پدیدآورنده
5. صفحه‌ی درباره‌ی سایت

## ترتیب ساخت
هر مرحله جداگانه انجام و تست شود، سپس commit شود.

1. راه‌اندازی پروژه‌ی Django و اپلیکیشن `rooms`
2. تعریف مدل‌ها و ثبت آن‌ها در Django Admin
3. تمپلیت پایه با RTL، فونت فارسی و منوی بالا
4. صفحه‌ی اصلی و صفحه‌ی دسته‌بندی با داده‌ی واقعی از دیتابیس
5. صفحه‌ی اثر با نمایش نظرات
6. فرم ارسال نظر
7. قابلیت پاسخ به نظر
8. جستجو
9. بهبود ظاهری و واکنش‌گرایی موبایل
10. آماده‌سازی برای انتشار روی لیارا

## مواردی که فعلاً پیاده نمی‌شوند
- ثبت‌نام و ورود کاربر
- لایک و امتیازدهی
- اعلان‌ها
- به‌روزرسانی لحظه‌ای بدون رفرش
- پروفایل کاربری

این‌ها بعد از اینکه نسخه‌ی اول کار کرد و کاربر واقعی داشت، بررسی می‌شوند.

## نکات مربوط به مدیریت محتوا
- حذف و تأیید نظرات از طریق Django Admin انجام می‌شود.
- فیلد `is_approved` امکان بررسی نظرات قبل از نمایش را می‌دهد.
- برای شروع، حدود ۲۰ اثر شناخته‌شده وارد شود؛ نه بیشتر.
